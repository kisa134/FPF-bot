"""Local secrets — gitignored, never pushed. Edit freely; this file is yours.

The key is hardcoded here so `python -m v1 run "<task>"` works with zero setup.
It is kept out of version control (.gitignore) so it can't leak from the repo.
"""

# WaveSpeed aggregator key (https://llm.wavespeed.ai/v1).
WAVESPEED_API_KEY = "wsk_live_BNHutHyDMHiHOmSPqPqsfxmQ_lIQbI4FVbE36y1nypo"
